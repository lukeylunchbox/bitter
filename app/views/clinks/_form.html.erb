<%= form_with(model: clink, local: true) do |form| %>
  <% if clink.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(clink.errors.count, "error") %> prohibited this clink from being saved:</h2>

      <ul>
      <% clink.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :clink %>
    <%= form.text_area :clink, id: :clink_clink %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
